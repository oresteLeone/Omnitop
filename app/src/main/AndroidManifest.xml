<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.annoyingturtle.omnitop">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Omnitop">
        <activity android:name=".GuidaSchedaDnDActivity"
            android:parentActivityName=".DndSchedaActivity"/>
        <activity android:name=".fragment.noteActivity.ModificaNota" />
        <activity android:name=".fragment.noteActivity.NuovaNota" />
        <activity android:name=".DndSchedaActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".DndHome"
            android:label="@string/dnd"
            android:parentActivityName=".HomeActivity" />
        <activity
            android:name=".DndCampagnaHome"
            android:label="Campagna DND"
            android:parentActivityName=".DndHome"/>
    </application>

</manifest>